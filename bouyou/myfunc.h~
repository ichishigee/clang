#include <stdio.h>

int max(int x, int y);


int main(void)
{

  int n1=560;
  int n2=80;
  int ans;

  ans=max(n1,n2);
  printf("%d %d %d\n", n1, n2, ans);
  
  return 0;
}

int max(int x, int y){
  if (x>y)
    return x;
  else
    return y;
}
